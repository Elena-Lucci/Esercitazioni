/*Crea una template string che calcola e stampa in console la somma di due numeri.*/
const num1 = 10;
const num2 = 20;
const sum = `La somma dei due numeri è ${num1 + num2}`;
console.log(sum);

/*Utilizza una template string per creare una stringa su più righe che includa interruzioni di riga. */
const soldati = `Si sta come
d'autunno
sugli alberi
le foglie`
console.log(soldati);

/*Crea una template string che genera un messaggio di saluto con un nome e un messaggio personalizzato.*/
const name = "John";
const message = "Welcome to our website!";
const greeting = `${name}, ${message.toLowerCase()}`;
console.log(greeting);

 